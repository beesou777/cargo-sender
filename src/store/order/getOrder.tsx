import { components } from "@/types/eurosender-api-types"
import { create } from "zustand"

type OrderRequesthiType = components["schemas"]["OrderRequest"]
const useGetAOrderStore = create((get, set) => {

})